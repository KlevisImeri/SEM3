```git
git commit
git branch
git branch -d <brach> //delete
git branch -f <brach> <destination> //move the branches
git switch
git checkout //this also allows you to move arond
git merge <branch> //merge a brach to this brach
git rebase <brach> //rebase this brach to the breach mentioned


git clone
git fetch
git pull  == git fetch git merge
git push
```

